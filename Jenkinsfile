pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {

            }
        }

        stage('Dependencies install') {
            steps {

            }
        }

        stage('Run Newman') {
            steps {

            }
        }

        stage('Rapport generate') {
            steps {
                
            }
        }
    }
}
