pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }

        stage('Dependencies install') {
            steps {

            }
        }

        stage('Run Newman') {
            steps {

            }
        }

        stage('Rapport generate') {
            steps {

            }
        }
    }
}
